// Code generated by the Encore 1.50.6 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/
import type { CookieWithOptions } from "encore.dev/api";

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the  Encore application.
 */
export class Client {
    public readonly music: music.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.music = new music.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    amapianorizeTrack as api_music_analyze_amapianorizeTrack,
    analyzeAudio as api_music_analyze_analyzeAudio,
    batchAnalyze as api_music_analyze_batchAnalyze,
    extractPatterns as api_music_analyze_extractPatterns,
    getAnalysisHistory as api_music_analyze_getAnalysisHistory,
    getBatchStatus as api_music_analyze_getBatchStatus,
    getUploadUrl as api_music_analyze_getUploadUrl
} from "~backend/music/analyze";
import {
    addComment as api_music_collaboration_addComment,
    collaborationSession as api_music_collaboration_collaborationSession,
    createCollaboration as api_music_collaboration_createCollaboration,
    createRemixChallenge as api_music_collaboration_createRemixChallenge,
    getCollaborationFeed as api_music_collaboration_getCollaborationFeed,
    joinCollaboration as api_music_collaboration_joinCollaboration,
    shareContent as api_music_collaboration_shareContent,
    submitRemix as api_music_collaboration_submitRemix
} from "~backend/music/collaboration";
import {
    generateDawElement as api_music_daw_generateDawElement,
    listProjects as api_music_daw_listProjects,
    loadProject as api_music_daw_loadProject,
    saveProject as api_music_daw_saveProject
} from "~backend/music/daw";
import {
    createLearningPath as api_music_education_createLearningPath,
    generateCulturalInsight as api_music_education_generateCulturalInsight,
    generateTutorial as api_music_education_generateTutorial,
    getCulturalInsights as api_music_education_getCulturalInsights,
    getEducationalContent as api_music_education_getEducationalContent,
    getLearningPaths as api_music_education_getLearningPaths,
    initializeBuiltinContent as api_music_education_initializeBuiltinContent
} from "~backend/music/education";
import {
    analyzeWithEssentia as api_music_essentia_api_analyzeWithEssentia,
    classifyRegion as api_music_essentia_api_classifyRegion,
    detectKwaitoInfluence as api_music_essentia_api_detectKwaitoInfluence,
    initializeStreamingAnalysis as api_music_essentia_api_initializeStreamingAnalysis,
    listAvailableModels as api_music_essentia_api_listAvailableModels,
    predictWithModel as api_music_essentia_api_predictWithModel
} from "~backend/music/essentia-api";
import {
    generateLoop as api_music_generate_generateLoop,
    generateTrack as api_music_generate_generateTrack,
    getGenerationHistory as api_music_generate_getGenerationHistory,
    getGenerationStats as api_music_generate_getGenerationStats
} from "~backend/music/generate";
import {
    health as api_music_health_health,
    readiness as api_music_health_readiness
} from "~backend/music/health";
import {
    createPattern as api_music_patterns_createPattern,
    deletePattern as api_music_patterns_deletePattern,
    getPattern as api_music_patterns_getPattern,
    getPatternsByComplexity as api_music_patterns_getPatternsByComplexity,
    getRecommendedPatterns as api_music_patterns_getRecommendedPatterns,
    listPatterns as api_music_patterns_listPatterns,
    searchPatternsByCulture as api_music_patterns_searchPatternsByCulture
} from "~backend/music/patterns";
import {
    generateFromTemplate as api_music_plugin_templates_generateFromTemplate,
    getGeneratedPlugin as api_music_plugin_templates_getGeneratedPlugin,
    getTemplate as api_music_plugin_templates_getTemplate,
    getTemplateAnalytics as api_music_plugin_templates_getTemplateAnalytics,
    listTemplates as api_music_plugin_templates_listTemplates,
    suggestTemplates as api_music_plugin_templates_suggestTemplates
} from "~backend/music/plugin-templates";
import {
    createProjectSnapshot as api_music_realtime_collaboration_createProjectSnapshot,
    getActiveCollaborators as api_music_realtime_collaboration_getActiveCollaborators,
    getCollaborationStats as api_music_realtime_collaboration_getCollaborationStats,
    getProjectVersionHistory as api_music_realtime_collaboration_getProjectVersionHistory,
    liveCollaboration as api_music_realtime_collaboration_liveCollaboration
} from "~backend/music/realtime-collaboration";
import {
    collectFeedback as api_music_research_api_collectFeedback,
    collectLearningExample as api_music_research_api_collectLearningExample,
    compareExperiments as api_music_research_api_compareExperiments,
    getBenchmarks as api_music_research_api_getBenchmarks,
    getCAQResults as api_music_research_api_getCAQResults,
    getDistriGenScalingAnalysis as api_music_research_api_getDistriGenScalingAnalysis,
    getExperiments as api_music_research_api_getExperiments,
    getPatternRecommendations as api_music_research_api_getPatternRecommendations,
    getResearchTimeSeries as api_music_research_api_getResearchTimeSeries,
    initPatternCache as api_music_research_api_initPatternCache,
    recordBenchmark as api_music_research_api_recordBenchmark,
    recordExperiment as api_music_research_api_recordExperiment,
    runCAQExperiment as api_music_research_api_runCAQExperiment,
    runDistriGenExperiment as api_music_research_api_runDistriGenExperiment,
    trackPatternUsage as api_music_research_api_trackPatternUsage,
    triggerModelAdaptation as api_music_research_api_triggerModelAdaptation
} from "~backend/music/research-api";
import {
    deleteSample as api_music_samples_deleteSample,
    getSample as api_music_samples_getSample,
    listSamples as api_music_samples_listSamples,
    processSample as api_music_samples_processSample,
    searchSamplesByCulture as api_music_samples_searchSamplesByCulture,
    uploadSample as api_music_samples_uploadSample
} from "~backend/music/samples";
import {
    deleteAudioFile as api_music_storage_deleteAudioFile,
    downloadAudioFile as api_music_storage_downloadAudioFile,
    getStorageStats as api_music_storage_getStorageStats,
    uploadAudioFile as api_music_storage_uploadAudioFile
} from "~backend/music/storage";

export namespace music {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addComment = this.addComment.bind(this)
            this.amapianorizeTrack = this.amapianorizeTrack.bind(this)
            this.analyzeAudio = this.analyzeAudio.bind(this)
            this.analyzeWithEssentia = this.analyzeWithEssentia.bind(this)
            this.batchAnalyze = this.batchAnalyze.bind(this)
            this.classifyRegion = this.classifyRegion.bind(this)
            this.collaborationSession = this.collaborationSession.bind(this)
            this.collectFeedback = this.collectFeedback.bind(this)
            this.collectLearningExample = this.collectLearningExample.bind(this)
            this.compareExperiments = this.compareExperiments.bind(this)
            this.createCollaboration = this.createCollaboration.bind(this)
            this.createLearningPath = this.createLearningPath.bind(this)
            this.createPattern = this.createPattern.bind(this)
            this.createProjectSnapshot = this.createProjectSnapshot.bind(this)
            this.createRemixChallenge = this.createRemixChallenge.bind(this)
            this.deleteAudioFile = this.deleteAudioFile.bind(this)
            this.deletePattern = this.deletePattern.bind(this)
            this.deleteSample = this.deleteSample.bind(this)
            this.detectKwaitoInfluence = this.detectKwaitoInfluence.bind(this)
            this.downloadAudioFile = this.downloadAudioFile.bind(this)
            this.extractPatterns = this.extractPatterns.bind(this)
            this.generateCulturalInsight = this.generateCulturalInsight.bind(this)
            this.generateDawElement = this.generateDawElement.bind(this)
            this.generateFromTemplate = this.generateFromTemplate.bind(this)
            this.generateLoop = this.generateLoop.bind(this)
            this.generateResearchReport = this.generateResearchReport.bind(this)
            this.generateTrack = this.generateTrack.bind(this)
            this.generateTutorial = this.generateTutorial.bind(this)
            this.getActiveCollaborators = this.getActiveCollaborators.bind(this)
            this.getAdaptationRecommendations = this.getAdaptationRecommendations.bind(this)
            this.getAnalysisHistory = this.getAnalysisHistory.bind(this)
            this.getBatchStatus = this.getBatchStatus.bind(this)
            this.getBenchmarks = this.getBenchmarks.bind(this)
            this.getCAQResults = this.getCAQResults.bind(this)
            this.getCacheStatistics = this.getCacheStatistics.bind(this)
            this.getCollaborationFeed = this.getCollaborationFeed.bind(this)
            this.getCollaborationStats = this.getCollaborationStats.bind(this)
            this.getCulturalInsights = this.getCulturalInsights.bind(this)
            this.getDistriGenScalingAnalysis = this.getDistriGenScalingAnalysis.bind(this)
            this.getEducationalContent = this.getEducationalContent.bind(this)
            this.getExperimentMetrics = this.getExperimentMetrics.bind(this)
            this.getExperiments = this.getExperiments.bind(this)
            this.getGeneratedPlugin = this.getGeneratedPlugin.bind(this)
            this.getGenerationHistory = this.getGenerationHistory.bind(this)
            this.getGenerationStats = this.getGenerationStats.bind(this)
            this.getLearningPaths = this.getLearningPaths.bind(this)
            this.getLearningStatistics = this.getLearningStatistics.bind(this)
            this.getPattern = this.getPattern.bind(this)
            this.getPatternRecommendations = this.getPatternRecommendations.bind(this)
            this.getPatternsByComplexity = this.getPatternsByComplexity.bind(this)
            this.getProjectVersionHistory = this.getProjectVersionHistory.bind(this)
            this.getRecommendedPatterns = this.getRecommendedPatterns.bind(this)
            this.getRecommenderStatistics = this.getRecommenderStatistics.bind(this)
            this.getResearchDashboard = this.getResearchDashboard.bind(this)
            this.getResearchSummary = this.getResearchSummary.bind(this)
            this.getResearchTimeSeries = this.getResearchTimeSeries.bind(this)
            this.getSample = this.getSample.bind(this)
            this.getStorageStats = this.getStorageStats.bind(this)
            this.getTemplate = this.getTemplate.bind(this)
            this.getTemplateAnalytics = this.getTemplateAnalytics.bind(this)
            this.getUploadUrl = this.getUploadUrl.bind(this)
            this.health = this.health.bind(this)
            this.initPatternCache = this.initPatternCache.bind(this)
            this.initializeBuiltinContent = this.initializeBuiltinContent.bind(this)
            this.initializeStreamingAnalysis = this.initializeStreamingAnalysis.bind(this)
            this.joinCollaboration = this.joinCollaboration.bind(this)
            this.listAvailableModels = this.listAvailableModels.bind(this)
            this.listPatterns = this.listPatterns.bind(this)
            this.listProjects = this.listProjects.bind(this)
            this.listSamples = this.listSamples.bind(this)
            this.listTemplates = this.listTemplates.bind(this)
            this.liveCollaboration = this.liveCollaboration.bind(this)
            this.loadProject = this.loadProject.bind(this)
            this.predictWithModel = this.predictWithModel.bind(this)
            this.processSample = this.processSample.bind(this)
            this.readiness = this.readiness.bind(this)
            this.recordBenchmark = this.recordBenchmark.bind(this)
            this.recordExperiment = this.recordExperiment.bind(this)
            this.runCAQExperiment = this.runCAQExperiment.bind(this)
            this.runDistriGenExperiment = this.runDistriGenExperiment.bind(this)
            this.saveProject = this.saveProject.bind(this)
            this.searchPatternsByCulture = this.searchPatternsByCulture.bind(this)
            this.searchSamplesByCulture = this.searchSamplesByCulture.bind(this)
            this.shareContent = this.shareContent.bind(this)
            this.submitRemix = this.submitRemix.bind(this)
            this.suggestTemplates = this.suggestTemplates.bind(this)
            this.trackPatternUsage = this.trackPatternUsage.bind(this)
            this.triggerModelAdaptation = this.triggerModelAdaptation.bind(this)
            this.uploadAudioFile = this.uploadAudioFile.bind(this)
            this.uploadSample = this.uploadSample.bind(this)
        }

        /**
         * Adds a comment to a feed item
         */
        public async addComment(params: RequestType<typeof api_music_collaboration_addComment>): Promise<ResponseType<typeof api_music_collaboration_addComment>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/collaboration/comment`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_collaboration_addComment>
        }

        /**
         * Enhanced amapianorize with cultural authenticity and quality metrics
         */
        public async amapianorizeTrack(params: RequestType<typeof api_music_analyze_amapianorizeTrack>): Promise<ResponseType<typeof api_music_analyze_amapianorizeTrack>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/analyze/amapianorize`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_analyze_amapianorizeTrack>
        }

        /**
         * Enhanced audio analysis with real AI processing
         */
        public async analyzeAudio(params: RequestType<typeof api_music_analyze_analyzeAudio>): Promise<ResponseType<typeof api_music_analyze_analyzeAudio>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/analyze/audio`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_analyze_analyzeAudio>
        }

        public async analyzeWithEssentia(params: RequestType<typeof api_music_essentia_api_analyzeWithEssentia>): Promise<ResponseType<typeof api_music_essentia_api_analyzeWithEssentia>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/essentia/analyze`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_essentia_api_analyzeWithEssentia>
        }

        public async batchAnalyze(params: RequestType<typeof api_music_analyze_batchAnalyze>): Promise<ResponseType<typeof api_music_analyze_batchAnalyze>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/analyze/batch`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_analyze_batchAnalyze>
        }

        public async classifyRegion(params: RequestType<typeof api_music_essentia_api_classifyRegion>): Promise<ResponseType<typeof api_music_essentia_api_classifyRegion>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/essentia/regional`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_essentia_api_classifyRegion>
        }

        /**
         * Real-time collaboration session for a DAW project.
         */
        public async collaborationSession(params: RequestType<typeof api_music_collaboration_collaborationSession>): Promise<StreamInOut<StreamRequest<typeof api_music_collaboration_collaborationSession>, StreamResponse<typeof api_music_collaboration_collaborationSession>>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                projectId: String(params.projectId),
            })

            return await this.baseClient.createStreamInOut(`/daw/collaboration/session`, {query})
        }

        public async collectFeedback(params: RequestType<typeof api_music_research_api_collectFeedback>): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/learning/feedback`, {method: "POST", body: JSON.stringify(params)})
        }

        public async collectLearningExample(params: RequestType<typeof api_music_research_api_collectLearningExample>): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/learning/collect`, {method: "POST", body: JSON.stringify(params)})
        }

        public async compareExperiments(params: RequestType<typeof api_music_research_api_compareExperiments>): Promise<void> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                baselineId:   params.baselineId,
                experimentId: params.experimentId,
            })

            await this.baseClient.callTypedAPI(`/research/experiments/compare`, {query, method: "GET", body: undefined})
        }

        /**
         * Creates a new collaboration space
         */
        public async createCollaboration(params: RequestType<typeof api_music_collaboration_createCollaboration>): Promise<ResponseType<typeof api_music_collaboration_createCollaboration>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/collaboration`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_collaboration_createCollaboration>
        }

        public async createLearningPath(params: RequestType<typeof api_music_education_createLearningPath>): Promise<ResponseType<typeof api_music_education_createLearningPath>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/education/learning-paths`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_education_createLearningPath>
        }

        /**
         * Create custom pattern
         */
        public async createPattern(params: RequestType<typeof api_music_patterns_createPattern>): Promise<ResponseType<typeof api_music_patterns_createPattern>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patterns`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_patterns_createPattern>
        }

        /**
         * Create a project snapshot/version
         */
        public async createProjectSnapshot(params: RequestType<typeof api_music_realtime_collaboration_createProjectSnapshot>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                createdBy:   params.createdBy,
                description: params.description,
            }

            await this.baseClient.callTypedAPI(`/collaboration/${encodeURIComponent(params.projectId)}/snapshot`, {method: "POST", body: JSON.stringify(body)})
        }

        /**
         * Creates a remix challenge
         */
        public async createRemixChallenge(params: RequestType<typeof api_music_collaboration_createRemixChallenge>): Promise<ResponseType<typeof api_music_collaboration_createRemixChallenge>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/collaboration/remix-challenge`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_collaboration_createRemixChallenge>
        }

        public async deleteAudioFile(params: { fileName: string }): Promise<ResponseType<typeof api_music_storage_deleteAudioFile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/storage/audio/${encodeURIComponent(params.fileName)}`, {method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_storage_deleteAudioFile>
        }

        /**
         * Delete pattern
         */
        public async deletePattern(params: { id: number }): Promise<ResponseType<typeof api_music_patterns_deletePattern>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patterns/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_patterns_deletePattern>
        }

        /**
         * Delete sample
         */
        public async deleteSample(params: { id: number }): Promise<ResponseType<typeof api_music_samples_deleteSample>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/samples/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_samples_deleteSample>
        }

        public async detectKwaitoInfluence(params: RequestType<typeof api_music_essentia_api_detectKwaitoInfluence>): Promise<ResponseType<typeof api_music_essentia_api_detectKwaitoInfluence>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/essentia/kwaito`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_essentia_api_detectKwaitoInfluence>
        }

        public async downloadAudioFile(params: { fileName: string }): Promise<ResponseType<typeof api_music_storage_downloadAudioFile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/storage/audio/${encodeURIComponent(params.fileName)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_storage_downloadAudioFile>
        }

        /**
         * Enhanced pattern extraction with expert-level analysis
         */
        public async extractPatterns(params: RequestType<typeof api_music_analyze_extractPatterns>): Promise<ResponseType<typeof api_music_analyze_extractPatterns>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/analyze/patterns`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_analyze_extractPatterns>
        }

        public async generateCulturalInsight(params: RequestType<typeof api_music_education_generateCulturalInsight>): Promise<ResponseType<typeof api_music_education_generateCulturalInsight>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/education/generate-insight`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_education_generateCulturalInsight>
        }

        /**
         * Generates a new track or clip for the DAW using AI.
         */
        public async generateDawElement(params: RequestType<typeof api_music_daw_generateDawElement>): Promise<ResponseType<typeof api_music_daw_generateDawElement>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/daw/ai-generate`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_daw_generateDawElement>
        }

        public async generateFromTemplate(params: RequestType<typeof api_music_plugin_templates_generateFromTemplate>): Promise<ResponseType<typeof api_music_plugin_templates_generateFromTemplate>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/plugins/generate`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_plugin_templates_generateFromTemplate>
        }

        /**
         * Enhanced loop generation with real AI processing
         */
        public async generateLoop(params: RequestType<typeof api_music_generate_generateLoop>): Promise<ResponseType<typeof api_music_generate_generateLoop>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/generate/loop`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_generate_generateLoop>
        }

        public async generateResearchReport(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/report`, {method: "GET", body: undefined})
        }

        /**
         * Enhanced track generation with real AI processing
         */
        public async generateTrack(params: RequestType<typeof api_music_generate_generateTrack>): Promise<ResponseType<typeof api_music_generate_generateTrack>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/generate/track`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_generate_generateTrack>
        }

        /**
         * API endpoints
         */
        public async generateTutorial(params: RequestType<typeof api_music_education_generateTutorial>): Promise<ResponseType<typeof api_music_education_generateTutorial>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/education/generate-tutorial`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_education_generateTutorial>
        }

        /**
         * Get active collaborators for a project
         */
        public async getActiveCollaborators(params: { projectId: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/collaboration/${encodeURIComponent(params.projectId)}/active`, {method: "GET", body: undefined})
        }

        public async getAdaptationRecommendations(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/learning/recommendations`, {method: "GET", body: undefined})
        }

        /**
         * Keep existing endpoints with enhanced error handling and validation
         */
        public async getAnalysisHistory(params: RequestType<typeof api_music_analyze_getAnalysisHistory>): Promise<ResponseType<typeof api_music_analyze_getAnalysisHistory>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                genre:      params.genre,
                limit:      params.limit === undefined ? undefined : String(params.limit),
                sortBy:     params.sortBy,
                sortOrder:  params.sortOrder === undefined ? undefined : String(params.sortOrder),
                sourceType: params.sourceType,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/analyze/history`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_analyze_getAnalysisHistory>
        }

        public async getBatchStatus(params: { batchId: string }): Promise<ResponseType<typeof api_music_analyze_getBatchStatus>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/analyze/batch/${encodeURIComponent(params.batchId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_analyze_getBatchStatus>
        }

        public async getBenchmarks(params: RequestType<typeof api_music_research_api_getBenchmarks>): Promise<void> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:      params.limit === undefined ? undefined : String(params.limit),
                systemName: params.systemName,
            })

            await this.baseClient.callTypedAPI(`/research/benchmarks`, {query, method: "GET", body: undefined})
        }

        public async getCAQResults(params: RequestType<typeof api_music_research_api_getCAQResults>): Promise<void> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                genre: params.genre === undefined ? undefined : String(params.genre),
                limit: params.limit === undefined ? undefined : String(params.limit),
            })

            await this.baseClient.callTypedAPI(`/research/caq/results`, {query, method: "GET", body: undefined})
        }

        public async getCacheStatistics(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/cache/statistics`, {method: "GET", body: undefined})
        }

        /**
         * Gets collaboration feed
         */
        public async getCollaborationFeed(params: RequestType<typeof api_music_collaboration_getCollaborationFeed>): Promise<ResponseType<typeof api_music_collaboration_getCollaborationFeed>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:    params.limit === undefined ? undefined : String(params.limit),
                offset:   params.offset === undefined ? undefined : String(params.offset),
                userName: params.userName,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/collaboration/${encodeURIComponent(params.collaborationId)}/feed`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_collaboration_getCollaborationFeed>
        }

        /**
         * Get collaboration statistics
         */
        public async getCollaborationStats(params: { projectId: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/collaboration/${encodeURIComponent(params.projectId)}/stats`, {method: "GET", body: undefined})
        }

        public async getCulturalInsights(params: RequestType<typeof api_music_education_getCulturalInsights>): Promise<ResponseType<typeof api_music_education_getCulturalInsights>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/education/cultural-insights`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_education_getCulturalInsights>
        }

        public async getDistriGenScalingAnalysis(params: RequestType<typeof api_music_research_api_getDistriGenScalingAnalysis>): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/distrigen/scaling`, {method: "POST", body: JSON.stringify(params)})
        }

        public async getEducationalContent(params: RequestType<typeof api_music_education_getEducationalContent>): Promise<ResponseType<typeof api_music_education_getEducationalContent>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category:   params.category,
                difficulty: params.difficulty,
                limit:      params.limit === undefined ? undefined : String(params.limit),
                offset:     params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/education/content`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_education_getEducationalContent>
        }

        public async getExperimentMetrics(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/experiments/metrics`, {method: "GET", body: undefined})
        }

        public async getExperiments(params: RequestType<typeof api_music_research_api_getExperiments>): Promise<void> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit: params.limit === undefined ? undefined : String(params.limit),
            })

            await this.baseClient.callTypedAPI(`/research/experiments`, {query, method: "GET", body: undefined})
        }

        public async getGeneratedPlugin(params: { pluginId: string }): Promise<ResponseType<typeof api_music_plugin_templates_getGeneratedPlugin>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/plugins/${encodeURIComponent(params.pluginId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_plugin_templates_getGeneratedPlugin>
        }

        /**
         * Keep existing endpoints with enhanced implementations
         */
        public async getGenerationHistory(params: RequestType<typeof api_music_generate_getGenerationHistory>): Promise<ResponseType<typeof api_music_generate_getGenerationHistory>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                filterBy:  params.filterBy === undefined ? undefined : String(params.filterBy),
                genre:     params.genre === undefined ? undefined : String(params.genre),
                limit:     params.limit === undefined ? undefined : String(params.limit),
                sortBy:    params.sortBy === undefined ? undefined : String(params.sortBy),
                sortOrder: params.sortOrder === undefined ? undefined : String(params.sortOrder),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/generate/history`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_generate_getGenerationHistory>
        }

        public async getGenerationStats(): Promise<ResponseType<typeof api_music_generate_getGenerationStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/generate/stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_generate_getGenerationStats>
        }

        public async getLearningPaths(params: RequestType<typeof api_music_education_getLearningPaths>): Promise<ResponseType<typeof api_music_education_getLearningPaths>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                difficulty: params.difficulty,
                limit:      params.limit === undefined ? undefined : String(params.limit),
                offset:     params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/education/learning-paths`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_education_getLearningPaths>
        }

        public async getLearningStatistics(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/learning/stats`, {method: "GET", body: undefined})
        }

        /**
         * Get pattern details
         */
        public async getPattern(params: { id: number }): Promise<ResponseType<typeof api_music_patterns_getPattern>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patterns/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_patterns_getPattern>
        }

        public async getPatternRecommendations(params: RequestType<typeof api_music_research_api_getPatternRecommendations>): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/patterns/recommend`, {method: "POST", body: JSON.stringify(params)})
        }

        /**
         * Get patterns by complexity
         */
        public async getPatternsByComplexity(params: RequestType<typeof api_music_patterns_getPatternsByComplexity>): Promise<ResponseType<typeof api_music_patterns_getPatternsByComplexity>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category: params.category === undefined ? undefined : String(params.category),
                genre:    params.genre === undefined ? undefined : String(params.genre),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patterns/complexity/${encodeURIComponent(params.level)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_patterns_getPatternsByComplexity>
        }

        /**
         * Project version history for synchronization
         */
        public async getProjectVersionHistory(params: { projectId: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/collaboration/${encodeURIComponent(params.projectId)}/versions`, {method: "GET", body: undefined})
        }

        /**
         * Get recommended patterns based on current project
         */
        public async getRecommendedPatterns(params: RequestType<typeof api_music_patterns_getRecommendedPatterns>): Promise<ResponseType<typeof api_music_patterns_getRecommendedPatterns>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                bpm:             params.bpm === undefined ? undefined : String(params.bpm),
                currentPatterns: params.currentPatterns?.map((v) => v === undefined ? undefined : String(v)),
                genre:           String(params.genre),
                keySignature:    params.keySignature,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patterns/recommendations`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_patterns_getRecommendedPatterns>
        }

        public async getRecommenderStatistics(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/patterns/stats`, {method: "GET", body: undefined})
        }

        public async getResearchDashboard(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/dashboard`, {method: "GET", body: undefined})
        }

        public async getResearchSummary(): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/summary`, {method: "GET", body: undefined})
        }

        public async getResearchTimeSeries(params: RequestType<typeof api_music_research_api_getResearchTimeSeries>): Promise<void> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                days: params.days === undefined ? undefined : String(params.days),
            })

            await this.baseClient.callTypedAPI(`/research/dashboard/timeseries`, {query, method: "GET", body: undefined})
        }

        /**
         * Get sample details
         */
        public async getSample(params: { id: number }): Promise<ResponseType<typeof api_music_samples_getSample>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/samples/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_samples_getSample>
        }

        public async getStorageStats(): Promise<ResponseType<typeof api_music_storage_getStorageStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/storage/stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_storage_getStorageStats>
        }

        public async getTemplate(params: { templateId: string }): Promise<ResponseType<typeof api_music_plugin_templates_getTemplate>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/plugins/templates/${encodeURIComponent(params.templateId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_plugin_templates_getTemplate>
        }

        public async getTemplateAnalytics(params: { templateId: string }): Promise<ResponseType<typeof api_music_plugin_templates_getTemplateAnalytics>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/plugins/templates/${encodeURIComponent(params.templateId)}/analytics`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_plugin_templates_getTemplateAnalytics>
        }

        /**
         * Enhanced upload URL generation with professional features
         */
        public async getUploadUrl(params: RequestType<typeof api_music_analyze_getUploadUrl>): Promise<ResponseType<typeof api_music_analyze_getUploadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/analyze/upload-url`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_analyze_getUploadUrl>
        }

        public async health(): Promise<ResponseType<typeof api_music_health_health>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/health`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_health_health>
        }

        public async initPatternCache(params: RequestType<typeof api_music_research_api_initPatternCache>): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/cache/init`, {method: "POST", body: JSON.stringify(params)})
        }

        /**
         * Built-in tutorial content
         */
        public async initializeBuiltinContent(): Promise<ResponseType<typeof api_music_education_initializeBuiltinContent>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/education/initialize`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_education_initializeBuiltinContent>
        }

        public async initializeStreamingAnalysis(params: RequestType<typeof api_music_essentia_api_initializeStreamingAnalysis>): Promise<ResponseType<typeof api_music_essentia_api_initializeStreamingAnalysis>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/essentia/streaming/init`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_essentia_api_initializeStreamingAnalysis>
        }

        /**
         * Joins an existing collaboration
         */
        public async joinCollaboration(params: RequestType<typeof api_music_collaboration_joinCollaboration>): Promise<ResponseType<typeof api_music_collaboration_joinCollaboration>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/collaboration/join`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_collaboration_joinCollaboration>
        }

        public async listAvailableModels(): Promise<ResponseType<typeof api_music_essentia_api_listAvailableModels>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/essentia/tensorflow/models`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_essentia_api_listAvailableModels>
        }

        /**
         * List available amapiano patterns
         */
        public async listPatterns(params: RequestType<typeof api_music_patterns_listPatterns>): Promise<ResponseType<typeof api_music_patterns_listPatterns>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                bpm:                  params.bpm === undefined ? undefined : String(params.bpm),
                category:             params.category === undefined ? undefined : String(params.category),
                complexity:           params.complexity === undefined ? undefined : String(params.complexity),
                culturalAuthenticity: params.culturalAuthenticity === undefined ? undefined : String(params.culturalAuthenticity),
                genre:                params.genre === undefined ? undefined : String(params.genre),
                keySignature:         params.keySignature,
                limit:                params.limit === undefined ? undefined : String(params.limit),
                offset:               params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patterns`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_patterns_listPatterns>
        }

        /**
         * Lists all saved DAW projects.
         */
        public async listProjects(): Promise<ResponseType<typeof api_music_daw_listProjects>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/daw/projects`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_daw_listProjects>
        }

        /**
         * List available amapiano samples
         */
        public async listSamples(params: RequestType<typeof api_music_samples_listSamples>): Promise<ResponseType<typeof api_music_samples_listSamples>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                bpm:          params.bpm === undefined ? undefined : String(params.bpm),
                category:     params.category === undefined ? undefined : String(params.category),
                genre:        params.genre === undefined ? undefined : String(params.genre),
                keySignature: params.keySignature,
                limit:        params.limit === undefined ? undefined : String(params.limit),
                offset:       params.offset === undefined ? undefined : String(params.offset),
                tags:         params.tags?.map((v) => v),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/samples`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_samples_listSamples>
        }

        public async listTemplates(params: RequestType<typeof api_music_plugin_templates_listTemplates>): Promise<ResponseType<typeof api_music_plugin_templates_listTemplates>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category: params.category === undefined ? undefined : String(params.category),
                genre:    params.genre,
                limit:    params.limit === undefined ? undefined : String(params.limit),
                verified: params.verified === undefined ? undefined : String(params.verified),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/plugins/templates`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_plugin_templates_listTemplates>
        }

        /**
         * Live collaboration stream endpoint - TODO: Fix streaming implementation
         */
        public async liveCollaboration(params: { projectId: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/collaboration/${encodeURIComponent(params.projectId)}/live`, {method: "POST", body: undefined})
        }

        /**
         * Loads a DAW project.
         */
        public async loadProject(params: { projectId: number }): Promise<ResponseType<typeof api_music_daw_loadProject>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/daw/projects/${encodeURIComponent(params.projectId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_daw_loadProject>
        }

        public async predictWithModel(params: RequestType<typeof api_music_essentia_api_predictWithModel>): Promise<ResponseType<typeof api_music_essentia_api_predictWithModel>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/essentia/tensorflow/predict`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_essentia_api_predictWithModel>
        }

        /**
         * Process sample (normalize, convert, etc.)
         */
        public async processSample(params: RequestType<typeof api_music_samples_processSample>): Promise<ResponseType<typeof api_music_samples_processSample>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                operations: params.operations,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/samples/${encodeURIComponent(params.sampleId)}/process`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_samples_processSample>
        }

        public async readiness(): Promise<ResponseType<typeof api_music_health_readiness>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/ready`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_health_readiness>
        }

        public async recordBenchmark(params: RequestType<typeof api_music_research_api_recordBenchmark>): Promise<ResponseType<typeof api_music_research_api_recordBenchmark>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/research/benchmarks`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_research_api_recordBenchmark>
        }

        public async recordExperiment(params: RequestType<typeof api_music_research_api_recordExperiment>): Promise<ResponseType<typeof api_music_research_api_recordExperiment>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/research/experiments`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_research_api_recordExperiment>
        }

        public async runCAQExperiment(params: RequestType<typeof api_music_research_api_runCAQExperiment>): Promise<ResponseType<typeof api_music_research_api_runCAQExperiment>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/research/caq/run`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_research_api_runCAQExperiment>
        }

        public async runDistriGenExperiment(params: RequestType<typeof api_music_research_api_runDistriGenExperiment>): Promise<ResponseType<typeof api_music_research_api_runDistriGenExperiment>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/research/distrigen/run`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_research_api_runDistriGenExperiment>
        }

        /**
         * Saves or updates a DAW project.
         */
        public async saveProject(params: RequestType<typeof api_music_daw_saveProject>): Promise<ResponseType<typeof api_music_daw_saveProject>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/daw/projects`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_daw_saveProject>
        }

        /**
         * Search patterns by cultural significance
         */
        public async searchPatternsByCulture(params: RequestType<typeof api_music_patterns_searchPatternsByCulture>): Promise<ResponseType<typeof api_music_patterns_searchPatternsByCulture>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                genre: params.genre === undefined ? undefined : String(params.genre),
                query: params.query,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patterns/search/cultural`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_patterns_searchPatternsByCulture>
        }

        /**
         * Search samples by cultural significance
         */
        public async searchSamplesByCulture(params: RequestType<typeof api_music_samples_searchSamplesByCulture>): Promise<ResponseType<typeof api_music_samples_searchSamplesByCulture>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                genre: params.genre === undefined ? undefined : String(params.genre),
                query: params.query,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/samples/search/cultural`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_samples_searchSamplesByCulture>
        }

        /**
         * Shares content in a collaboration
         */
        public async shareContent(params: RequestType<typeof api_music_collaboration_shareContent>): Promise<ResponseType<typeof api_music_collaboration_shareContent>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/collaboration/share`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_collaboration_shareContent>
        }

        /**
         * Submits a remix to a challenge
         */
        public async submitRemix(params: RequestType<typeof api_music_collaboration_submitRemix>): Promise<ResponseType<typeof api_music_collaboration_submitRemix>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/collaboration/remix-challenge/submit`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_collaboration_submitRemix>
        }

        public async suggestTemplates(params: RequestType<typeof api_music_plugin_templates_suggestTemplates>): Promise<ResponseType<typeof api_music_plugin_templates_suggestTemplates>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/plugins/suggest`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_plugin_templates_suggestTemplates>
        }

        public async trackPatternUsage(params: RequestType<typeof api_music_research_api_trackPatternUsage>): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/patterns/track`, {method: "POST", body: JSON.stringify(params)})
        }

        public async triggerModelAdaptation(params: RequestType<typeof api_music_research_api_triggerModelAdaptation>): Promise<void> {
            await this.baseClient.callTypedAPI(`/research/learning/adapt`, {method: "POST", body: JSON.stringify(params)})
        }

        /**
         * API endpoints for storage management
         */
        public async uploadAudioFile(params: RequestType<typeof api_music_storage_uploadAudioFile>): Promise<ResponseType<typeof api_music_storage_uploadAudioFile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/storage/audio/upload`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_storage_uploadAudioFile>
        }

        /**
         * Upload custom sample
         */
        public async uploadSample(params: RequestType<typeof api_music_samples_uploadSample>): Promise<ResponseType<typeof api_music_samples_uploadSample>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/samples/upload`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_music_samples_uploadSample>
        }
    }
}


type PickMethods<Type> = Omit<CallParameters, "method"> & { method?: Type };

// Helper type to omit all fields that are cookies.
type OmitCookie<T> = {
  [K in keyof T as T[K] extends CookieWithOptions<any> ? never : K]: T[K];
};

type RequestType<Type extends (...args: any[]) => any> =
  Parameters<Type> extends [infer H, ...any[]]
    ? OmitCookie<H>
    : void;

type ResponseType<Type extends (...args: any[]) => any> = OmitCookie<Awaited<ReturnType<Type>>>;

function dateReviver(key: string, value: any): any {
  if (
    typeof value === "string" &&
    value.length >= 10 &&
    value.charCodeAt(0) >= 48 && // '0'
    value.charCodeAt(0) <= 57 // '9'
  ) {
    const parsedDate = new Date(value);
    if (!isNaN(parsedDate.getTime())) {
      return parsedDate;
    }
  }
  return value;
}


function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

import {
  StreamInOutHandlerFn,
  StreamInHandlerFn,
  StreamOutHandlerFn,
} from "encore.dev/api";

type StreamRequest<Type> = Type extends
  | StreamInOutHandlerFn<any, infer Req, any>
  | StreamInHandlerFn<any, infer Req, any>
  | StreamOutHandlerFn<any, any>
  ? Req
  : never;

type StreamResponse<Type> = Type extends
  | StreamInOutHandlerFn<any, any, infer Resp>
  | StreamInHandlerFn<any, any, infer Resp>
  | StreamOutHandlerFn<any, infer Resp>
  ? Resp
  : never;


function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data, dateReviver))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}


// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "-Generated-TS-Client (Encore/1.50.6)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(path: string, params?: CallParameters): Promise<Response> {
        return this.callAPI(path, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(path: string, params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}

export default new Client(import.meta.env.VITE_CLIENT_TARGET, { requestInit: { credentials: "include" } });
